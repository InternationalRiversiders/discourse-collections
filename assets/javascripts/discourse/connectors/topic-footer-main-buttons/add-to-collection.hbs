{{! 只有登录用户才显示 }}
{{#if currentUser}}
  <DButton
    @class="btn-default add-to-collection"
    @icon="folder-plus"
    @label="collections.add_to_collection"
    @action={{action "openCollectionModal"}}
    @title="collections.add_to_collection_title"
  />
{{/if}}